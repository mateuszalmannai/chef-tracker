<li class="{{if chef.hereToday '' 'not-cooking'}}">
  <div {{action 'toggleOpen'}}>{{chef.name}}{{if isOpen 'V' '>'}}</div>
  {{#if isOpen}}
    {{#link-to 'chefs.chef' chef.id}}<button>Show page</button>{{/link-to}}
    <button {{action 'toggleAvailability' chef}}>
      {{if chef.hereToday 'Exit' 'Enter'}}
    </button>
  {{/if}}
</li>

